name: esahay
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  http: ^1.2.2
  provider: ^6.0.0

dev_dependencies:
#  flutter_native_splash: ^2.4.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

#flutter_native_splash:
#  color: "#ffffff"
#  image: assets/logo.png
#  android: true
#  android_gravity: center # Control the alignment of the image (center, fill, etc.)
  # ios: true # Uncomment if you want to enable splash for iOS
  # ios_content_mode: scaleAspectFill # Optional for iOS splash image mode

flutter:
  uses-material-design: true

  # Add assets to your application:
  assets:
    - assets/logo.png
    - assets/images.png
  # You can add more assets as needed
